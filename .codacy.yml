---
engines:
  tsqllint:
    enabled: false

exclude_paths:
  - ".env"
  - ".env.*"
  - "dist/**"
  - "node_modules/**"
  - "src/components/ui/**"
  - "supabase/migrations/**"
  # shadcn/ui generated hook — no-confusing-void-expression false positives, not ours to change
  - "src/hooks/use-toast.ts"
