---
engines:
  tsqllint:
    enabled: false

exclude_paths:
  - ".env"
  - ".env.*"
  - "dist/**"
  - "node_modules/**"
  - "src/components/ui/**"
  - "supabase/migrations/**"
  # shadcn/ui generated hook — no-confusing-void-expression false positives, not ours to change
  - "src/hooks/use-toast.ts"
  # DOMPurify-sanitized innerHTML/dangerouslySetInnerHTML — not a real XSS risk, scanners can't see DOMPurify
  - "src/hooks/useBlockEditor.ts"
  - "src/pages/SharedPageView.tsx"
